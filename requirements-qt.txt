# Isolated desktop UI environment. Keep the legacy Tkinter environment unchanged.
# Core/Gui/Widgets/Svg are sufficient for this staged desktop migration.
# Avoid the much larger Addons package (3D, WebEngine, multimedia, etc.).
PySide6-Essentials>=6.8,<6.11
numpy==2.2.6
opencv-python==4.12.0.88
PyYAML>=6.0
pytest>=7.4
